// Sentinel checkout URL returned by the mock cart when Shopify is not
// configured. The cart drawer uses it to show a "connect Shopify" notice
// instead of redirecting.
export const MOCK_CHECKOUT_URL = "#mock-checkout";
